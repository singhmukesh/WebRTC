/**
 *   Modern specific extensions for auth
 */
Ext.define('WebRTC.controller.Auth', {
    extend: 'WebRTC.controller.GlobalAuth',
    alias: 'controller.auth',
    id: 'auth'

    // removed all toolkit specific auth for now
    // however it would go here.
});
